import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "AskExperts.io | AI Experts Powered by Bitcoin",
  description: "Ask AI Experts trained with real human knowledge. Self-hosted. Open protocol. Lightning payments built-in.",
  keywords: "AI, Bitcoin, Lightning Network, Experts, Open Protocol, Self-hosted",
  icons: {
    icon: '/logo.jpeg',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
